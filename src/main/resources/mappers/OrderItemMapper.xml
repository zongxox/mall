<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mall.mapper.OrderItemMapper">
    <!--建立訂單-->
    <insert id="insertOrderItem">
        INSERT INTO order_items (order_id, product_variant_id, quantity, price)
        VALUES (#{orderId}, #{productVariantId}, #{quantity}, #{price})
    </insert>

    <!--基於訂單id找到該用戶所有訂單明細-->
    <select id="userByOrderId" resultType="com.example.mall.entity.OrderItem">
        SELECT id, order_id, product_variant_id, quantity, price
        FROM order_items
        WHERE order_id = #{orderId}
    </select>
</mapper>